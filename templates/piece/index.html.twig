{% extends 'base.html.twig' %}

{% block title %}Piece index{% endblock %}

{% block body %}
    <h1>Piece index</h1>
<div id='pieces'></div>
    {# <table class="table w-100">
        <thead>
            <tr>
                
                <th>Nom</th>
                <th>vehicule</th>
                <th>Image</th>
                <th>Description</th>
                <th>stock</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for piece in pieces %}
            <tr>
                
                <td>{{ piece.nom }}</td>
              <td>{{  piece.idVehicule.marque }}  {{  piece.idVehicule.annee }}</td>
                 <td><img src="{{ asset('images/vehicule/' ~ piece.imageName) }}" width="10%"alt="piéce Image"/></td>
                <td>{{ piece.Description }}</td>
                <td>{{ piece.QuantiteEnStock }}</td>
                <td>{{ piece.prix }}</td>
                <td>
                    <a href="{{ path('app_piece_show', {'id': piece.id}) }}">show</a>
                    <a href="{{ path('app_piece_edit', {'id': piece.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    <a href="{{ path('app_piece_new') }}">Create new</a>
{% endblock %}
